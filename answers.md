1. What is the difference between git clone and git init?
   
Ans. git clone clones the existing Github repository in your computer.
git init initializes a git repository within an existing directory

2. What does git status tell you?
   
Ans. The command git status provides information about the current branch along with modified, staged, and untracked files.

3. What is the purpose of git add?
   
Ans. git add adds the changed files to the staging area before committing.

4. Difference between git diff and git diff --staged?

Ans. git diff shows unstaged changes, while git diff --staged shows staged changes.

5. What is the purpose of git commit?

Ans. git commit saves the staged changes in Git.

6. Difference between git log and git log --oneline?

Ans. git log shows detailed commit history, while git log --oneline shows it in a short form.

7. What information can you see using git show?

Ans. It shows the details and changes of a particular commit.

8. What does git pull do?

Ans. git pull gets the latest changes from GitHub to the local repository.

9. What does git push do?

Ans.  push sends our local commits to GitHub.

10. What is a Git branch?

Ans. A branch is a separate version of the project where we can work on changes.

11. What does git checkout do?

Ans. git checkout is used to switch between branches.

12. Why do developers create feature branches?

Ans. To work on new features without disturbing the main branch.

13. What is a Pull Request?

Ans. A Pull Request is a request to add changes from one branch to another.

14. Why should a Pull Request be reviewed before merging?

Ans. To check for mistakes and make sure the changes are correct.

15. Difference between main and feature-pages?

Ans. main is the main branch, while feature-pages is used to work on the pages separately.
